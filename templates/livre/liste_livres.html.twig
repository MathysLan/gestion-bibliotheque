<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des Livres</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        h1 {
            text-align: center;
            padding: 20px;
            margin-top: 20px;
            color: #333;
        }

        .book-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            margin: 0 auto;
            max-width: 1200px;
            padding: 20px;
        }

        .book-card {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin: 15px;
            padding: 20px;
            width: 250px;
            text-align: center;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .book-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        .book-title {
            font-size: 1.2em;
            font-weight: bold;
            margin: 10px 0;
            color: #333;
        }

        .book-author {
            font-size: 1em;
            color: #777;
            margin-bottom: 8px;
        }

        .book-genre {
            font-size: 0.9em;
            color: #555;
            background-color: #e0e0e0;
            padding: 5px;
            border-radius: 4px;
            display: inline-block;
            margin-bottom: 10px;
        }

        .book-description {
            font-size: 0.9em;
            color: #666;
            line-height: 1.5;
        }
    </style>
</head>
<body>

<h1>Liste des Livres</h1>

<div class="book-container">
    {% for livre in livres %}
        <div class="book-card">
            <div class="book-title">{{ livre.titre }}</div>
            <div class="book-author">Auteur : {{ livre.auteur.nom }} {{ livre.auteur.prenom }}</div>
            <div class="book-genre">Genre : {{ livre.genre.nom }}</div>
            <div class="book-description">{{ livre.description | slice(0, 100) }}{% if livre.description|length > 100 %}...{% endif %}</div>
        </div>
    {% else %}
        <p>Aucun livre trouvé.</p>
    {% endfor %}
</div>

</body>
</html>
